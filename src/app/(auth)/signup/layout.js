export const metadata = {
  title: "Sign up free",
  description:
    "Create a free OWE DUE account to track credits, debits, due history, events, and reminders in one premium workspace.",
  alternates: { canonical: "/signup" },
  robots: { index: true, follow: true },
};

export default function SignupLayout({ children }) {
  return children;
}
