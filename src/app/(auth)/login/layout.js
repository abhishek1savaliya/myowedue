export const metadata = {
  title: "Log in",
  description:
    "Sign in to your OWE DUE dashboard to manage people, transactions, reminders, reports, files, and subscription.",
  alternates: { canonical: "/login" },
  robots: { index: true, follow: true },
};

export default function LoginLayout({ children }) {
  return children;
}
